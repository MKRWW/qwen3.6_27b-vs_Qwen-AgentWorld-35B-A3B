# Kopiere nach .env und fülle aus. .env ist gitignored — NIEMALS committen.

# --- Qwen3.6-27b @ zuse (LAN) ---
# vLLM lokal akzeptiert i.d.R. einen beliebigen nicht-leeren Key.
QWEN_API_KEY=dummy-local-key

# --- AgentWorld-35B-A3B @ vast.ai ---
# Laut Setup-UI: Bearer-Token = $OPEN_BUTTON_TOKEN/$WEB_PASSWORD (mit Schrägstrich).
# Trage hier den fertigen Bearer-Wert ein (das, was nach "Bearer " steht).
AGENTWORLD_API_KEY=

# Optional: separate Bestandteile, falls du sie im Code zusammensetzen willst.
OPEN_BUTTON_TOKEN=
WEB_PASSWORD=
